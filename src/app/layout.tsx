import './layout.css'
import "./globals.css"
import { ReactNode } from "react"
import Flex from "@/components/Flex"
import type { Metadata } from "next"

export const metadata: Metadata = {
	description: "Generated by create next app",
	title: "Create Next App",
}


export interface Props {
	children: ReactNode
}

export default function Layout({ children }: Props) {
	return (
		<html>
			<body>
				<header>
					<Flex>
						<Flex vertical>
							<span className='header-title'>tmcnab.dev</span>
							<span className='header-subtitle'>engineer, husband, socialist</span>
						</Flex>
						<Flex>
							<select>
								<option>index</option>
								<option>about</option>
							</select>
							<select>
								<option>blog</option>
								<option>2024-09-21 • Something Devestating</option>
							</select>
							<button>GH</button>
						</Flex>
					</Flex>
				</header>
				<main>
					{children}
				</main>
			</body>
		</html >
	)
}
