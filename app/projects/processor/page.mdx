# Tranport Triggered Architecture

Every computer today in your possession almost invariable uses one of two processor
architectures: **Harvard** or **von Neumann**. A Harvard architecture processor
separates instruction memories from data memories, whereas a von Neumann one combines
both. A Northbridge would separate the CPU from the main memory and a Southbridge
would facilitate mapping data from peripherals to memory address banks.