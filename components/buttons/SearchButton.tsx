import { Button } from "../ui/button"
import { IconSearch } from "@tabler/icons-react"

const SearchButton = () => {
	return (
		<Button>
			<IconSearch />
		</Button>
	)
}

export default SearchButton